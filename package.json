{
  "scripts": {
    "open": "npx cypress open",
    "start": "npx cypress run",
    "on-tests-headless": "npx cypress run --browser chrome && cypress run --browser firefox && cypress run --browser edge",
    "on-chrome": "cypress run --browser chrome",
    "on-firefox": "cypress run --browser firefox",
    "on-edge": "cypress run --browser edge",
    "updateToLatestDevDependenciesAtNewPC": "npm i -g npm-check-updates && ncu -u && npm install",
    "updateToLatestDevDependencies": "ncu -u && npm install",
    "triggerRunner": "npx cypress run --record --key 3113fbb5-65c2-4e16-a98e-f05ba38f1a50 --browser chrome"
  },
  "devDependencies": {
    "typescript": "^4.7.3",
    "lodash": "^4.17.21"
  },
  "repository": {
    "type": "git",
    "url": "git+https://bitbucket.org/b2ssolution/automation.git"
  },
  "dependencies": {
    "mochawesome": "^7.1.3",
    "cypress": "^10.3.0",
    "cypress-mochawesome-reporter": "^3.2.0"
  }
}
