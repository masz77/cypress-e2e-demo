{
  "scripts": {
    "start-test-runner": "npx cypress open",
    "run all test in cli": "npx cypress run"
  },
  "devDependencies": {
    "cypress": "^9.5.4",
    "cypress-mochawesome-reporter": "^3.0.1"
  }
}
